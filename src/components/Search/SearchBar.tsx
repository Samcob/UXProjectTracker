const SearchBar = () => {
    return(
        <>
            <div className="flex items-center justify-center bg-white w-auto h-10 text-red-500 rounded-full">
                Search Bar: I still need to work on this
            </div>
        </>
    );
}

export default SearchBar